# offer-coach 产品需求文档（PRD）

> 撰写日期：2026-09-18｜阶段：Vibe Coding 五步工作流 第 2 步（需求）
> 依据：`research.md`（Day 3 产出）。范围决策（含 11 条「不做」）已在研究阶段由作者拍板，本文档原样继承。
> 技术选型（用什么工具实现）不在本文档范围内，Day 5 决定；本文档只约束「做什么、做到什么程度」。

## 一、产品概述

**一句话定位**：offer-coach 是一个网页工具——先帮你看清有哪些岗位、每个岗位要什么（本期覆盖技术方向），再把你看到的**具体岗位要求**和**你的学习进度**做对比，输出「已达标什么 / 还缺什么 / 先学什么」。

**产品形态（本期约束）**：
- 纯前端静态网页，通过 GitHub Pages 上线，有公开可访问链接
- 不需要登录，数据存在用户自己的浏览器里
- 中文界面

**要解决的两个连续问题**（来自研究）：
1. 岗位探索：「想做技术相关工作，但不知道具体有哪些岗位」→ 用岗位清单解决
2. 差距分析：「看到心仪岗位，说不清自己离它多远」→ 用差距分析解决

## 二、目标用户

**主用户**：想进入某个行业但方向尚不清晰的自学者与在校学生（第一个用户就是作者本人）。

**两个典型使用场景**：

| 场景 | 用户状态 | 用什么功能 |
|---|---|---|
| 场景 A：我还很迷茫 | 想做技术，但不知道有哪些岗位、各自要会什么 | 岗位清单 → 岗位详情，逐个看 |
| 场景 B：我有目标岗位了 | 在招聘软件看到一个具体岗位，想知道自己差在哪 | 差距分析：选预设岗位，或直接粘贴那段岗位描述 |

## 三、MVP 功能（本期做什么）

> 格式约定：每个功能按「用户动作 → 可观察结果」描述。可观察结果 = 用户在页面上能亲眼看到/亲手验证的现象。

### F1 浏览岗位清单

- **用户动作**：打开网站首页
- **可观察结果**：页面上显示 6-8 个岗位卡片（前端、后端、测试、数据分析、运维等常见技术岗），每张卡片至少包含：岗位名称、一句话说明（这个岗位干什么）；点击卡片能进入该岗位的详情页

### F2 查看岗位详情并标记「我会」

- **用户动作**：点击某张岗位卡片进入详情页；在技能清单里勾选自己已掌握的技能
- **可观察结果**：详情页展示该岗位的职责说明 + 技能清单，每个技能旁有可勾选的标记；勾选后该技能立即呈现「已掌握」样式（如打勾/变色）；勾选状态在关闭浏览器后重新打开仍保留（见 F3）

### F3 我的进度（本地保存）

- **用户动作**：勾选任意技能后，关闭浏览器或电脑，之后重新打开网站
- **可观察结果**：之前的所有勾选状态原样恢复，不需要重新登录或重新勾选；（数据存在浏览器本地存储中，不依赖账号）

### F4 差距分析——从预设岗位进入

- **用户动作**：在差距分析页选择一个预设岗位（或从岗位详情页一键进入分析）
- **可观察结果**：页面输出三栏结果——**已达标**（我勾过的、该岗位要求的技能）、**缺口**（该岗位要求但我没勾的）、**建议学习顺序**（缺口按基础→进阶排列）；三栏内容与我的勾选状态一致，每一条都能看出对应的技能名

### F5 差距分析——粘贴任意岗位描述

- **用户动作**：在差距分析页的输入框里，粘贴从招聘软件复制的岗位描述文字（来源可以是手动复制，或系统截图提字后粘贴），点击「分析」
- **可观察结果**：不依赖预设岗位，直接输出同样的三栏结果；识别出的每个缺口项都能对应到岗位描述里的相关文字或技能名（结果可解释，不是黑盒）；本期实现方式（关键词匹配或接入 AI）由 Day 5 技术选型决定，PRD 只约束上述结果

### F6 补充自定义技能

- **用户动作**：在「我的进度」区域手动输入技能总表里没有的技能名（如「我还会 Git」），确认添加
- **可观察结果**：新技能出现在我的技能列表里，与预设技能同等对待——参与差距比对、同样可勾选状态管理

### F7 上线发布

- **用户动作**：在浏览器访问 GitHub Pages 的公开链接
- **可观察结果**：不经过任何本地环境，网页可直接打开并完成 F1-F6 的全部操作

## 四、后续功能（以后再做）与本期明确不做

### 下一期方向（结构上已预留）

| 功能 | 说明 | 为什么现在不做 |
|---|---|---|
| 网站内截图自动识别（OCR） | 拖图进网页自动出文字 | 系统自带提字已解决 80% 的录入需求，省下 1-2 天开发投给核心功能 |
| 非技术方向岗位内容 | 设计、产品、运营等 | 结构和数据支持扩展，本期只填技术方向内容 |
| 岗位智能推荐 | 按兴趣/背景推荐岗位 | 需要数据积累和算法，静态清单先跑通结构 |
| 学习中状态 | 技能除「会/不会」外增加「学习中」 | 增加状态复杂度，先验证核心链路 |

### 本期明确不做（继承 research.md 的 11 条，理由见该文档第五节）

爬取招聘网站｜网站内 OCR｜账号与登录｜简历生成优化｜题库刷题｜自动投递与平台接口｜薪资与推荐算法｜移动端 App｜多语言｜岗位智能推荐｜非技术方向内容

> **砍掉功能的核心理由（今日掌握点素材）**：本期砍掉的不是「坏功能」，而是「28 天内性价比不高的功能」——比如网站内 OCR：开发要 1-2 天，而系统截图提字这步免费且已经够用；账号登录：单人自用场景下本地存储完全够，登录反而增加注册流失和维护成本。

## 五、页面与交互要求

> 共 4 个页面（含首页），导航简单，文字优先，不追求视觉效果。

### P1 首页（= 岗位清单页）

- **页面上有什么**：网站名与一句话定位；岗位卡片列表（F1）
- **交互**：点卡片 → 进入 P2；导航栏有「岗位」「差距分析」「我的进度」三个入口

### P2 岗位详情页

- **页面上有什么**：该岗位的职责说明；该岗位的技能清单（每项可勾选）；「用这个岗位做差距分析」按钮
- **交互**：勾选 → 状态立即变化并自动保存（F2/F3）；点分析按钮 → 进入 P3 并带上该岗位

### P3 差距分析页

- **页面上有什么**：两种入口（下拉选择预设岗位 / 大文本框粘贴岗位描述 + 「分析」按钮）；结果区三栏（已达标 / 缺口 / 建议学习顺序）；「去补充我的技能」快捷入口
- **交互**：选岗位或粘贴文字后点分析 → 展示三栏结果（F4/F5）；输入为空或过短时给出提示（见第七节）

### P4 我的进度

- **页面上有什么**：我已掌握的技能汇总列表；添加自定义技能的输入框（F6）；已存数据量的说明（如「共标记 12 项技能」）
- **交互**：可在此取消勾选；添加自定义技能后立即出现在列表

### 全局要求

- 页面在手机宽度下不排版错乱（响应式，不做 App）
- 所有按钮点击后有可见反馈，不存在「点了没反应」的死按钮
- 界面全部中文，用语口语化（目标用户是零基础学习者）

## 六、数据字段

> 本期所有数据存浏览器本地存储；字段是「内容的清单」，不含实现细节。

**岗位（job）**——静态内容，随代码发布：

| 字段 | 含义 | 示例 | 必填 |
|---|---|---|---|
| 岗位名 | 卡片标题 | 前端开发 | 是 |
| 一句话简介 | 卡片上的说明 | 做用户能看见、能点的界面 | 是 |
| 职责说明 | 详情页展示 | 负责网页界面实现与交互效果 | 是 |
| 关联技能 | 该岗位要求的技能清单 | [HTML/CSS, JavaScript, …] | 是 |

**技能（skill）**——静态总表 + 用户数据：

| 字段 | 含义 | 示例 | 必填 |
|---|---|---|---|
| 技能名 | 唯一标识 | JavaScript | 是 |
| 分类 | 语言/框架/工具/其他 | 语言 | 是 |
| 学习阶段 | 基础/进阶/高级，用于差距分析的「建议学习顺序」排序 | 基础 | 是 |
| 一句话说明 | 零基础能懂的注解 | 让网页动起来的编程语言 | 是 |
| 关联岗位 | 多对多：这个技能被哪些岗位要求 | [前端开发, 测试开发] | 是 |
| 来源 | 预设 / 用户自定义 | 预设 | 是 |

**我的进度（progress）**——用户数据，本地保存：

| 字段 | 含义 | 示例 |
|---|---|---|
| 技能名 | 对应技能总表 | JavaScript |
| 是否已掌握 | 勾选状态 | true / false |

**自定义技能**——同技能字段，「关联岗位」可为空（空 = 只参与粘贴岗位描述的比对，不挂在预设岗位上）。

**差距分析输入与输出**——输入为岗位描述文本（临时使用，不强制保存）；输出为三栏列表：已达标项（技能名）、缺口项（技能名 + 出处原文）、建议顺序（缺口按基础→进阶排序）。

## 七、异常状态

> 原则：任何异常都给「人话提示」，不允许白屏或无反应。

| 异常情况 | 网站应该怎么做 |
|---|---|
| 粘贴的岗位描述为空或太短（少于 20 字） | 按钮点击后提示「请粘贴完整的岗位描述再分析」，不执行分析 |
| 岗位描述里识别不出任何技能 | 显示「没有从这段文字里认出技能要求，请检查内容或换个岗位试试」，不显示空三栏 |
| 岗位描述里出现了技能总表没有收录的技能 | 该技能直接列入「缺口」栏并标注「总表外技能」，同时提示「如果你已掌握此项，可用自定义技能添加并勾选」——总表管「网站认识哪些技能」，我的进度管「我会哪些」，用户通过自定义技能认领，不改总表 |
| 用户还没勾任何技能就做分析 | 正常输出三栏，但「已达标」栏显示空 + 提示「你还没有标记任何已掌握技能，可先去勾选」 |
| 本地存储被清空（清缓存/换浏览器/隐私模式） | 网站不报错不崩溃，以「全部未勾选」的默认状态运行；在我的进度页提示「进度保存在此浏览器中，换浏览器或清缓存会丢失」 |
| 添加重名的自定义技能 | 提示「这个技能已经存在」，不产生重复条目 |
| 页面资源加载失败（如网络问题） | 显示友好的错误提示与重试引导，不白屏 |

## 八、验收标准（可逐条打勾）

> 验收方式：每条都是「打开页面做某操作 → 应看到某结果」，全部通过 = MVP 合格。

| # | 验收项 | 通过标准 |
|---|---|---|
| 1 | 岗位清单 | 打开首页，能看到 6-8 个岗位卡片，每张有岗位名和一句话说明 |
| 2 | 岗位详情 | 点击任一卡片，能进入详情页并看到该岗位的技能清单 |
| 3 | 进度保存 | 勾选 3 个技能 → 完全关闭浏览器 → 重新打开网站，3 个勾选仍在 |
| 4 | 预设岗位分析 | 在差距分析页选择一个预设岗位（或从岗位详情页点击「用这个岗位做差距分析」按钮进入），能看到「已达标/缺口/建议学习顺序」三栏，内容与勾选状态一致 |
| 5 | 任意岗位分析 | 粘贴一段真实招聘软件的岗位描述文字并点分析，能输出三栏结果，且不依赖预设岗位 |
| 6 | 结果可解释 | 缺口栏里的每一项，都能看出它对应的技能名（以及来自岗位描述的出处） |
| 7 | 自定义技能 | 添加一个总表里没有的技能后，它出现在我的进度里，并参与下次差距分析 |
| 8 | 异常提示 | 粘贴空白内容点分析，出现提示文字而不是报错或白屏 |
| 9 | 本地存储丢失 | 清除浏览器本地存储后刷新，网站正常可用，显示默认空状态 |
| 10 | 在线可访问 | 通过 GitHub Pages 公开链接，在没登录任何账号的浏览器里能完成以上全部操作 |
| 11 | 界面约束 | 全程无登录环节、无付费环节、界面为中文 |
| 12 | 多对多生效 | 勾选一个被多个岗位要求的技能（如 JavaScript），分别打开其中两个岗位的详情页，两处都显示已掌握 |

## 九、本文档的边界

- 不含技术选型（框架、实现方式）——Day 5 决定
- 不含视觉设计稿——设计阶段产出
- 「差距分析」的实现路径（关键词匹配 / AI 接口）本文档不指定，只约束第八节第 4-6 条的结果要求
